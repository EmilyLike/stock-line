import { useStore, Dispatch } from 'vuex'
import { defineComponent, ref } from 'vue'
import { useRouter, Router } from 'vue-router'

export default defineComponent({
  setup() {
    return () => <div class="main">single</div>
  }
})
